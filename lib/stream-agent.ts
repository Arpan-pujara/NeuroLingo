/** Fixed id — must match vision-agent `NEUROLINGO_AGENT_USER_ID` (do not read env on client). */
export const NEUROLINGO_AGENT_USER_ID = "neuro-lingo-teacher";

export const NEUROLINGO_AGENT_USER_NAME = "NeuroLingo Teacher";
