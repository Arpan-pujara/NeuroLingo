[project]
name = "neuro-lingo-teacher"
version = "0.1.0"
description = "NeuroLingo voice-only AI language teacher (Vision Agents + Ollama + Stream Edge)"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "python-dotenv>=1.2.2",
    "vision-agents[fast-whisper,getstream,kokoro,openai]>=0.6.1",
]
